let x = 10;
const y = 5;

console.log(x + y);

const a = 'a';
const b = '2';
const c = 2;

console.log(a + b);
console.log(b + c);
console.log(x + y + a + c); // 15a2

// Arithmetic operators
// - * / %

// Logical operators
// ! && ||

// Binary operators
// & | !

// Assignment shortcuts
x = x + 1;
x += 1;
x++;

// Ternary operator: condition ? do if true : do if false
const result = x < 10 ? 1 : 0;
console.log('Result: ', result);

// Comparisons
// > < >= <=

// Equality
// === Strict equality, type and value
// == Allows type coercion for value comparison
